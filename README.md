
  # Online Ordering Web App

  This is a code bundle for Online Ordering Web App. The original project is available at https://www.figma.com/design/Ku0MVfmlpJPFGztYq0LUOd/Online-Ordering-Web-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  